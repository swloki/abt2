---
paths:
  - "abt-web/**/*.rs"
---

# Hyperscript 语法用法（薄壳 → omp rulebook 源）

本文件是 Claude Code 的路径门控入口，正文单一源在 omp rulebook：

@../../.omp/rules/hyperscript-patterns.md

如未自动展开，请用 read 工具读取 `../../.omp/rules/hyperscript-patterns.md`。
