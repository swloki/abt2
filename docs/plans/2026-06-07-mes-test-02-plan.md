# MES-02 生产计划

> **路由前缀**: `/admin/mes/plans`
> **代码文件**: `mes_plan_list.rs`, `mes_plan_create.rs`, `mes_plan_detail.rs`
> **路由文件**: `abt-web/src/routes/mes_plan.rs`
> **服务**: `abt-core/src/mes/production_plan/`
> **原型文件**: `04-plan-list.html`, `04-plan-create.html`, `04-plan-detail.html`

---

## 1. 计划列表页

### 1.1 页面访问

| ID | 测试项 | 操作 | 预期结果 |
|----|--------|------|---------|
| PL-01 | 直接访问 | GET /admin/mes/plans | 200，页面标题 "生产计划" |
| PL-02 | Dashboard 快捷入口 | 点击快捷入口"生产计划" | 跳转 /admin/mes/plans |
| PL-03 | 侧栏导航 | 点击侧栏"生产计划" | 跳转 /admin/mes/plans |
| PL-04 | 面包屑 | 查看面包屑 | 显示 "生产管理 > 生产计划" |

### 1.2 页面头部

| ID | 测试项 | 预期结果 |
|----|--------|---------|
| PL-10 | 页面标题 | 显示 "生产计划" |
| PL-11 | 新建按钮 | 右侧 "新建计划" 按钮，链接到 /admin/mes/plans/create |

### 1.3 筛选区域

| ID | 筛选项 | 控件类型 | 参数名 | 原型选项 |
|----|--------|---------|--------|---------|
| PL-20 | 关键词搜索 | search-wrap + input | keyword | placeholder="搜索计划编号…" |
| PL-21 | 状态筛选 | select | status | 全部 / 草稿 / 已确认 / 进行中 / 已完成 / 已取消 |
| PL-22 | 类型筛选 | select | plan_type | 全部类型 / 按单生产MTO / 按库存备货MTS |
| PL-23 | 开始日期 | date input | date_from | — |
| PL-24 | 结束日期 | date input | date_to | — |

> **原型对比**: 原型有状态 Tab 栏（全部18/草稿4/已确认6/进行中5/已完成2/已取消1），实现用 select 筛选。

### 1.4 筛选交互

| ID | 测试项 | 操作 | 预期结果 |
|----|--------|------|---------|
| PL-30 | 关键词搜索 | 输入 "PP-" | HTMX 触发请求（delay:300ms），表格只显示匹配行 |
| PL-31 | 状态筛选 | 选择 "草稿" | 表格只显示 Draft 状态计划 |
| PL-32 | 类型筛选 | 选择 "MTO" | 表格只显示 Mto 类型计划 |
| PL-33 | 日期范围 | 设置 date_from 和 date_to | 按日期范围过滤 |
| PL-34 | 组合筛选 | 同时设置多个条件 | 所有条件 AND 组合 |
| PL-35 | 筛选+翻页 | 筛选后翻页 | 筛选条件保留 |
| PL-36 | 清空搜索 | 删除搜索内容 | 恢复显示全部 |

### 1.5 数据表格

**表头列（实现）**:

| 列 | 对齐 | 可点击 | 说明 |
|----|------|--------|------|
| 计划编号 | 左/mono/蓝色 | ✅ | 点击跳转详情 |
| 计划日期 | 左 | — | — |
| 排产类型 | 左 | — | 带颜色 pill（MTO蓝/MTS紫） |
| 状态 | 左 | — | 带颜色 pill |
| 创建人 | 左 | — | 解析为用户名 |
| 创建时间 | 左/灰色 | — | YYYY-MM-DD HH:mm |
| 操作 | 左 | ✅ | "查看"链接 |

**原型额外列（对比）**:

| 原型列 | 实现状态 | 说明 |
|--------|---------|------|
| 计划行数 | ❌ 缺失 | 原型显示行数（如 5） |
| 关联销售单 | ❌ 缺失 | 原型有链接到销售单 |

### 1.6 表格交互

| ID | 测试项 | 操作 | 预期结果 |
|----|--------|------|---------|
| PL-40 | 行点击 | 点击表格行 | 跳转到 /admin/mes/plans/{id} |
| PL-41 | 查看链接 | 点击"查看" | 跳转详情 |
| PL-42 | 空数据 | 无计划数据 | "暂无生产计划" 居中提示 |
| PL-43 | 状态标签颜色 | 不同状态行 | 颜色正确（见枚举值速查） |
| PL-44 | 类型标签 | MTO/MTS 行 | 蓝色/紫色 pill |
| PL-45 | 分页 | 数据 > 20 条 | 显示分页组件 |
| PL-46 | 创建人显示 | — | 显示 display_name 非 ID |

---

## 2. 新建计划页

### 2.1 页面访问

| ID | 测试项 | 操作 | 预期结果 |
|----|--------|------|---------|
| PL-60 | 从列表进入 | 点击"新建计划"按钮 | 跳转 /admin/mes/plans/create |
| PL-61 | 页面标题 | — | "新建生产计划" |
| PL-62 | 返回链接 | 点击"← 返回列表" | 跳回 /admin/mes/plans |
| PL-63 | 面包屑 | — | "生产管理 > 生产计划 > 新建计划" |

### 2.2 基本信息表单

| ID | 字段 | 控件 | name | 必填 | 原型设计 | 实现差异 |
|----|------|------|------|------|---------|---------|
| PL-70 | 计划编号 | readonly input | — | — | 自动编号 PP-2026-XX-XXXXX | 可能缺失 |
| PL-71 | 计划日期 | date input | plan_date | ✅ | 默认今日 | — |
| PL-72 | 排产类型 | select | plan_type | ✅ | MTO/MTS | — |
| PL-73 | 生产中心 | select | — | — | A线SMT/B线组装/C线测试 | ❌ 缺失 |
| PL-74 | 备注 | textarea | remark | — | placeholder="输入备注信息…" | — |

### 2.3 计划明细区域

动态表格，通过 JS 添加行。

**实现列**:

| 列 | 控件 | data-field | 必填 |
|----|------|-----------|------|
| 序号 | 自动编号 | — | — |
| 产品 | number input | product_id | ✅ (placeholder="产品ID") |
| 计划数量 | number input | planned_qty | ✅ (step=0.01) |
| 开始日期 | date input | scheduled_start | ✅ |
| 结束日期 | date input | scheduled_end | ✅ |
| 优先级 | number input | priority | 默认 1 |
| 删除 | button | — | — |

**原型额外列（对比）**:

| 原型列 | 实现状态 | 说明 |
|--------|---------|------|
| 产品选择器 | ❌ 缺失 | 原型用 select 下拉选择产品 |
| 关联销售单 | ❌ 缺失 | 原型有 select 选择 SO |
| BOM | ❌ 缺失 | 原型有 select 选择 BOM |
| 工艺路线 | ❌ 缺失 | 原型有 select 选择工艺路线 |
| 优先级 | ⚠️ number | 原型用 select（高/中/低） |

### 2.4 创建交互测试

| ID | 测试项 | 操作 | 预期结果 |
|----|--------|------|---------|
| PL-80 | 添加行 | 点击"添加计划行" | tbody 增加一行空表单 |
| PL-81 | 多次添加 | 连续点击 3 次 | 3 行，序号 1/2/3 |
| PL-82 | 删除行 | 点击行尾 ✕ | 该行被移除，序号重排 |
| PL-83 | 空提交 | 不填任何字段直接提交 | HTML5 校验阻止 |
| PL-84 | 只填基本信息 | 填类型和日期，不添加明细行 | 提交成功（items_json=[]） |
| PL-85 | 完整提交 | 所有字段 + 1 行明细 | hx-post → HX-Redirect 到列表 |
| PL-86 | items_json 检查 | 提交前检查 hidden input | JSON 数组，每行含 product_id/planned_qty/scheduled_start/scheduled_end/priority |
| PL-87 | 取消 | 点击"取消" | 跳回列表，不创建 |
| PL-88 | 日期格式 | 输入无效日期 | 服务端返回错误 |
| PL-89 | 产品 ID 验证 | 输入不存在的产品 ID | 服务端返回错误或创建成功但显示"—" |

> **原型对比**: 原型有"保存草稿"和"确认提交"两个按钮，实现可能只有一个提交按钮。

---

## 3. 计划详情页

### 3.1 页面访问

| ID | 测试项 | 操作 | 预期结果 |
|----|--------|------|---------|
| PL-90 | 从列表进入 | 点击计划行 | 跳转 /admin/mes/plans/{id} |
| PL-91 | 返回链接 | 点击"返回生产计划列表" | 跳回列表 |
| PL-92 | 无效 ID | /admin/mes/plans/999999 | 错误页或提示 |

### 3.2 详情头部

| ID | 元素 | 说明 |
|----|------|------|
| PL-93 | 计划编号 | detail-no class, mono 大号字体 |
| PL-94 | 状态 pill | 与列表一致的颜色标签 |

### 3.3 计划信息卡片

info-card + info-grid 布局：

| ID | 字段 | 样式 | 原型字段 |
|----|------|------|---------|
| PL-95 | 计划编号 | mono | — |
| PL-96 | 计划日期 | mono | — |
| PL-97 | 排产类型 | 文本 | MTO/MTS |
| PL-98 | 状态 | 颜色 pill | — |
| PL-99 | 创建人 | 文本 | 解析 display_name |
| PL-100 | 创建时间 | mono | YYYY-MM-DD HH:mm |

**原型额外字段**:

| 原型字段 | 实现状态 | 说明 |
|---------|---------|------|
| 生产中心 | ❌ 缺失 | 如"A 线 — SMT 车间" |

### 3.4 备注区域

| ID | 测试项 | 预期结果 |
|----|--------|---------|
| PL-105 | 有备注 | 显示备注卡片 |
| PL-106 | 无备注 | 不显示备注卡片 |

### 3.5 计划明细表格

**实现列**:

| 列 | 说明 |
|----|------|
| 序号 | 1-based |
| 产品名称 | 解析 product_id → 名称 |
| 计划数量 | 右对齐 |
| 计划开始 | 日期 |
| 计划结束 | 日期 |
| 优先级 | 右对齐 |
| 状态 | 颜色 pill |

**原型额外列**:

| 原型列 | 实现状态 | 说明 |
|--------|---------|------|
| 关联 | ❌ 缺失 | 关联销售单 + 工单链接 |
| BOM/工艺 | ❌ 缺失 | BOM 版本 + 工艺路线 |

| ID | 测试项 | 预期结果 |
|----|--------|---------|
| PL-110 | 空明细 | "暂无计划明细" |
| PL-111 | 产品名解析 | 显示名称，不存在则"—" |

### 3.6 状态操作按钮

#### 确认计划（Draft → Confirmed）

| ID | 测试项 | 预期结果 |
|----|--------|---------|
| PL-120 | 按钮可见性 | Draft 状态显示"确认计划"按钮 |
| PL-121 | 确认弹窗 | 点击后弹窗"确认此生产计划？确认后计划将进入已确认状态。" |
| PL-122 | 确认操作 | hx-post /admin/mes/plans/{id}/confirm |
| PL-123 | 确认成功 | 状态变"已确认"，出现"下达计划"按钮 |
| PL-124 | 非 Draft | 不显示确认按钮 |

#### 下达计划（Confirmed → InProgress）

| ID | 测试项 | 预期结果 |
|----|--------|---------|
| PL-130 | 按钮可见性 | Confirmed 状态显示"下达计划"按钮 |
| PL-131 | 下达弹窗 | "下达此生产计划？将根据计划行生成对应工单。" |
| PL-132 | 下达操作 | hx-post /admin/mes/plans/{id}/release |
| PL-133 | 下达成功 | 状态变"进行中" |
| PL-134 | 工单生成 | 检查工单列表是否生成了对应工单 |
| PL-135 | 下达结果 | BatchReleaseResult 包含 successful_work_orders 和 failed_items |
| PL-136 | 非 Confirmed | 不显示下达按钮 |

### 3.7 完整状态流转

```
Draft → Confirmed → InProgress → Completed
  ↘                                  ↗
   ↠ Cancelled ↠
```

---

## 4. 缺陷跟踪

| ID | 问题描述 | 优先级 | 状态 |
|----|---------|--------|------|
| PL-BUG-01 | 产品选择用手动输入 ID，原型用 select 下拉 | P2 | 记录 |
| PL-BUG-02 | 创建表单缺少生产中心字段 | P2 | 记录 |
| PL-BUG-03 | 创建明细缺少关联销售单/BOM/工艺路线选择 | P2 | 记录 |
| PL-BUG-04 | 原型有"保存草稿"和"确认提交"两按钮 | P3 | 记录 |
| PL-BUG-05 | 列表缺少"计划行数"和"关联销售单"列 | P2 | 记录 |
| PL-BUG-06 | 详情缺少生产中心字段 | P3 | 记录 |
| PL-BUG-07 | 详情明细缺少关联和BOM/工艺列 | P2 | 记录 |
