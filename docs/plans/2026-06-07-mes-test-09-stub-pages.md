# MES 模块 4 个 Stub 页面测试报告

**测试日期**: 2026-06-07
**测试范围**: 流转卡查询、排程看板、物料消耗追踪、生产异常（4 个页面）
**测试数据**: 新增异常测试数据 6 条 + 事件 14 条，关联工单 BOM 数据
**原型对比**: Open Design 本地文件（04-*.html）

## 测试总览

| 页面 | 路径 | 状态 | 修复项 |
|------|------|------|--------|
| 流转卡查询 | /admin/mes/cards | ✅ | 工时格式化 |
| 排程看板 | /admin/mes/schedule | ✅ | — |
| 物料消耗追踪 | /admin/mes/material-usage | ✅ | SQL 修复（completed_qty 列不存在） |
| 生产异常列表 | /admin/mes/exceptions | ✅ | — |
| 异常详情 | /admin/mes/exceptions/:id | ✅ | 时间线翻译、发现人/负责人 lookup |

## 缺陷修复记录

### P0 阻塞

| # | 问题 | 修复 | 文件 |
|---|------|------|------|
| 1 | 物料消耗 HTMX 端点 500：`get_bom_comparison` SQL 引用不存在的 `wo.completed_qty` 列 | 改为子查询从 `production_batches` 聚合；修复 `p.pdt_code` → `p.product_code` | `abt-core/src/mes/dashboard/repo.rs` |

### P1 严重

| # | 问题 | 修复 | 文件 |
|---|------|------|------|
| 2 | 异常详情时间线事件类型显示英文原始值（如 `repair_in_progress`） | 添加 `event_type_label()` 翻译函数，覆盖 7 种事件类型 | `abt-web/src/pages/mes_exception_detail.rs` |

### P2 一般

| # | 问题 | 修复 | 文件 |
|---|------|------|------|
| 3 | 流转卡查询报工表格工时显示 `8.000000`，缺少单位 | 使用 `fmt_qty()` 格式化 + 添加 "h" 后缀 | `abt-web/src/pages/mes_card_query.rs` |
| 4 | 异常详情发现人/负责人显示 "—"（测试数据 finder_id=1 不存在） | 修正测试数据 user_id：1→6（admin） | 测试数据 |

### P3 轻微

| # | 问题 | 说明 |
|---|------|------|
| 5 | 流转卡查询缺少"扫描二维码"按钮 | 原型有，实现省略 |
| 6 | 流转卡查询缺少"最近查询"区域 | 原型有 6 个最近查询卡片，实现省略 |
| 7 | 排程看板卡片缺少 MTO/MTS 标签和延期风险标签 | 原型有，实现省略 |

## 数据验证结果

### 流转卡查询

- 搜索 FC-2026-06-000001 → 正确返回批次信息 ✅
- 信息网格：卡号、批次号、工单号、产品名、数量、工序进度 ✅
- 工序进度：4 道工序（SMT贴片→回流焊接→功能测试→包装入库）✅
- 报工明细：4 条记录，列名和数据正确 ✅
- 空搜索（NOTEXIST999）→ 显示"未找到流转卡"提示 ✅

### 排程看板

- 统计卡片：活跃工单 3、待排产 1、进行中 3、待入库 1、已完成 2 ✅
- 看板 4 列正确分组：待排产(1)、进行中(3)、待入库(1)、已完成(2) ✅
- 卡片内容：批次号、产品名（非 ID）、数量、当前工序、工单号 ✅
- 排除已取消批次（status=6）✅

### 物料消耗追踪

- 工单选择器：8 个非取消工单可选 ✅
- HTMX 数据加载：选择工单后异步加载（修复后 200）✅
- 工单信息网格：5 个字段 ✅
- 4 个统计卡：BOM 标准用量、倒冲消耗、用量差异、差异率 ✅
- BOM 对比表：关联 BOM 后正确显示物料行 ✅
- 倒冲记录：1 条测试记录显示正确 ✅
- 无 BOM 时显示提示文案 ✅

### 生产异常

- 统计卡片：本月异常 6、批次暂停 1、报废批次 1、报检不合格 1 ✅
- 数据表：6 条异常，7 列（编号/类型/关联/描述/影响数量/发现时间/状态）✅
- 类型筛选：选择"批次暂停"后过滤为 1 条 ✅
- 异常类型标签：批次暂停/批次报废/不良异常/报检不合格/设备故障 ✅
- 状态标签：待处理/处理中/已关闭 ✅
- 关联链接：工单号和批次号可点击跳转 ✅
- 空值处理：影响数量为 NULL 时显示 "—" ✅

### 异常详情

- 信息网格：12 个字段完整显示 ✅
- 产品名/工单号/批次号：显示可读名称而非 ID ✅
- 发现人/负责人：修复后显示 "Admin" ✅
- 异常描述：多行文本 ✅
- 处理时间线：4 条事件按时间倒序，中文翻译正确 ✅
- 返回列表链接 ✅

## 与原型对比差异

### 流转卡查询（04-card-query.html）

| 区域 | 原型 | 实现 | 差异 |
|------|------|------|------|
| 搜索区标题+描述 | ✅ | ✅ | — |
| 搜索输入+查询按钮 | ✅ | ✅ | — |
| 扫描二维码按钮 | ✅ | ❌ | P3 缺少 |
| 信息网格 | 8 项 | 6 项 | 缺少班组、BOM版本、工艺路线 |
| 工序进度条 | 水平步骤+详细信息 | 水平步骤+完成/不良 | 简化 |
| 报工明细表 | 9 列 | 8 列 | 缺少"计件工资"列 |
| 最近查询区域 | 6 个卡片 | ❌ | P3 缺少 |

### 排程看板（04-schedule-board.html）

| 区域 | 原型 | 实现 | 差异 |
|------|------|------|------|
| 统计卡片 5 个 | ✅ | ✅ | — |
| 看板 4 列 | ✅ | ✅ | — |
| 卡片进度条 | ✅ | 需确认 | — |
| MTO/MTS 标签 | ✅ | ❌ | P3 |
| 延期风险标签 | ✅ | ❌ | P3 |

### 物料消耗追踪（04-material-usage.html）

| 区域 | 原型 | 实现 | 差异 |
|------|------|------|------|
| 工单选择器 | ✅ | ✅ | — |
| 工单头部信息 | ✅ | ✅ | — |
| 4 个统计卡 | ✅ | ✅ | — |
| BOM 对比表 | ✅ | ✅ | — |
| 差异标签颜色 | 红绿灰 | 纯数字 | P3 缺少 diff-indicator 样式 |
| 倒冲明细表 | ✅ | ✅ | — |

### 生产异常（04-exception-list.html + 04-exception-detail.html）

| 区域 | 原型 | 实现 | 差异 |
|------|------|------|------|
| 统计卡片 4 个 | ✅ | ✅ | — |
| 状态 Tab 栏 | 6 个 Tab | 类型下拉 | 交互形式不同，功能等价 |
| 数据表 7 列 | ✅ | ✅ | — |
| 详情信息网格 12 字段 | ✅ | ✅ | — |
| 处理时间线 | ✅ | ✅ | — |
| 关联信息表 | ✅ | ❌ | P2 缺少 |
| 操作按钮（转交/关闭） | ✅ | ❌ | P2 缺少 |

## 测试数据

本次测试新增以下数据：

```sql
-- 生产异常 6 条（5 种类型：批次暂停/报废/不良/报检不合格/设备故障）
-- 异常事件 14 条（时间线）
-- 工单 1 关联 BOM 1000862（物料消耗追踪验证）
```

用户 ID 修正：`finder_id/owner_id/operator_id` 从 1 改为 6（admin 实际 user_id）。
