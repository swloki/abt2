{
  "name": "abt-e2e",
  "private": true,
  "type": "module",
  "description": "ABT Playwright E2E 回归测试套件（连本地运行的 ABT 服务）",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.0.0",
    "dotenv": "^16.4.7"
  }
}
